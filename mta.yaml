_schema-version: "3.2"
ID: comgitproject
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comgitproject-destination-content
  type: com.sap.application.content
  requires:
  - name: comgitproject-destination-service
    parameters:
      content-target: true
  - name: comgitproject-repo-host
    parameters:
      service-key:
        name: comgitproject-repo-host-key
  - name: comgitproject-uaa
    parameters:
      service-key:
        name: comgitproject-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comgitproject_html_repo_host
          ServiceInstanceName: comgitproject-html5-srv
          ServiceKeyName: comgitproject-repo-host-key
          sap.cloud.service: comgitproject
        - Authentication: OAuth2UserTokenExchange
          Name: comgitproject_uaa
          ServiceInstanceName: comgitproject-xsuaa-srv
          ServiceKeyName: comgitproject-uaa-key
          sap.cloud.service: comgitproject
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comgitproject-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comgitproject-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comgitproject.zip
      name: comgitproject
      target-path: resources/
- name: comgitproject
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comgitproject-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comgitproject-destination-service
    service-plan: lite
- name: comgitproject-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comgitproject-xsuaa-srv
    service-plan: application
- name: comgitproject-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comgitproject-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
